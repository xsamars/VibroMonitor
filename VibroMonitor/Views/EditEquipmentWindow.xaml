<Window x:Class="VibroMonitor.Views.EditEquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VibroMonitor.Views"
        xmlns:viewmodels="clr-namespace:VibroMonitor.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:EditEquipmentViewModel}"
        mc:Ignorable="d"
        Title="Редактирование оборудования" Height="300" Width="500" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название -->
        <TextBlock Grid.Row="0" Text="Название оборудования:" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Height="40" FontSize="14" Margin="0,0,0,16" Padding="8"/>

        <!-- Путь к изображению -->
        <TextBlock Grid.Row="2" Text="Путь к изображению:" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBox Grid.Row="3" Text="{Binding ImagePath, UpdateSourceTrigger=PropertyChanged}" Height="40" FontSize="14" Margin="0,0,0,16" Padding="8"/>

        <!-- Кнопки -->
        <Grid Grid.Row="5" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Сохранить" Width="120" Height="40" Margin="0,0,8,0" 
                    Command="{Binding SaveCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Background="#4CAF50" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand"/>

            <Button Grid.Column="2" Content="Отмена" Width="120" Height="40"
                    Command="{Binding CancelCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Background="#f44336" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand"/>
        </Grid>
    </Grid>
</Window>
