<Window x:Class="VibroMonitor.Views.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Пароль" Height="160" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Введите пароль администратора:" />
        <PasswordBox Grid.Row="1" x:Name="Pwd" Height="32" Margin="0,8,0,8" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Height="30">
            <Button Width="90" Margin="0,0,8,0" Content="OK" Click="Ok_Click" />
            <Button Width="90" Content="Отмена" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>