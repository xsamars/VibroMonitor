<Window x:Class="VibroMonitor.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сменить пароль" Height="230" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Новый пароль:" />
        <PasswordBox Grid.Row="1" x:Name="NewPwd" Height="32" Margin="0,8,0,8" />
        <TextBlock Grid.Row="2" Text="Подтверждение:" />
        <PasswordBox Grid.Row="3" x:Name="ConfirmPwd" Height="32" Margin="0,8,0,8" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Height="30">
            <Button Width="90" Margin="0,0,8,0" Content="Сохранить" Click="Save_Click" />
            <Button Width="90" Content="Отмена" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>